# AI-Chatbot-Project
Built an intelligent, context-aware customer support chatbot using Python. It uses the pre-trained microsoft/DialoGPT-medium NLP transformer model to generate human-like conversation responses based on multi-turn dialogue history. All user interactions are automatically recorded with structured timestamps into a local SQLite database log.
